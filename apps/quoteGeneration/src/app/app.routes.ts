import { Routes } from '@angular/router';
import { QuoteGenerationPage } from './quote-generation-page/quote-generation-page';

export const routes: Routes = [{ path: '', component: QuoteGenerationPage }];
